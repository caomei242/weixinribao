# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-21 10:49 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不手工执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`2026-05-21｜测试审查｜本地消息明细真实内容展示与群名可读回填复验`
- 复验对象：后端/API `2026-05-21｜后端API｜本地消息明细真实内容展示与群名可读回填`；前台/UI `2026-05-21｜前台UI｜本地消息明细真实内容展示与群名可读回填`
- 结论：完成通过。源码 / 测试合同、前台字段名对齐、重启后的 8765 运行态字段级 smoke 已闭合；本地 UI payload 已返回正文 / 预览字段，safe/report/smoke 与跨线程记录仍保持不摘录正文。
- 回传状态：已追加监工回报队列；send_input 调用失败，返回 `agent not found`，未取得 submission_id。

## 审查范围

- 后端/API状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 前台/UI状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/前台UI助手.md`
- 监工回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/app.js`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/index.html`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_local_ui_display_contract.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_windows_p0_backend.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_next_round_product_backend.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_persistent_real_read_contract.py`
- 运行态 smoke：`.codex_runtime_logs/http-smoke-local-ui-content-20260521-104821.log`

## 测试 / 证据

- 后端/API给定证据：新增专项 `.codex_py_logs/py-run-20260521-103724.log`，8 passed；相关后端 `.codex_py_logs/py-run-20260521-103800.log`，44 passed；全量 pytest `.codex_py_logs/py-run-20260521-103808.log`，151 passed；`git diff --check` exit=0。
- 后端/API状态卡追加证据：新增 / 持续授权群名解析专项 `.codex_py_logs/py-run-20260521-104006.log`，30 passed；相关后端专项 `.codex_py_logs/py-run-20260521-104012.log`，75 passed；全量 pytest `.codex_py_logs/py-run-20260521-104023.log`，151 passed；`git diff --check` exit=0。
- 前台/UI证据：状态卡 2026-05-21 10:41 CST 字段名对齐复核完成；`node --check src/wechat_feedback_app/static/app.js` exit=0；静态三文件 `git diff --check` exit=0；全局 `git diff --check` exit=0。
- 监工补证：运行态 smoke `.codex_runtime_logs/http-smoke-local-ui-content-20260521-104821.log` 已存在。
- 本轮补测：`/api/messages/v1` HTTP 200，`status=ok`，`message_count=6`，`content_text/content_preview/message_text/summary/content_status/content_returned` 字段存在行数均为 6；`local_ui_payload=true`，`content_returned=true`，`raw_payload_returned=false`，`report_safe_payload=false`，`raw_payload_key_present=false`。
- 本轮补测：`/api/config-center` HTTP 200，forbidden key hit count=0。
- 本轮补测：`git diff --check` exit=0。

## 本地 UI 正文 / 预览字段复核

- 通过：`messages_v1_payload()` 为本地 UI 消息项生成 `content_text`、`content_preview`、`message_text`、`summary`、`content_status`、`content_returned`。
- 通过：运行态 6 行消息均返回上述字段。
- 通过：`message_ref` 仍是技术引用 / `detail_target` 定位，不作为摘要字段。
- 通过：同项保留 `content_text_safe` / `content_preview_safe` / `summary_safe`，供 report-safe 场景使用。

## safe / report / smoke 分层复核

- 通过：本地 UI payload 明确 `local_ui_payload=true`、`content_returned=true`。
- 通过：运行态 `raw_payload_returned=false`、`report_safe_payload=false`，未返回 raw payload key。
- 通过：`/api/config-center` forbidden key hit count=0。
- 通过：状态卡、回报队列和本轮 smoke 只记录 count/status/error_code、字段存在性、布尔结果和日志路径；未摘录真实正文、真实群名或敏感路径。

## 群名可读回填与内部 ID 兜底复核

- 通过：有本地 session/contact/chatroom metadata 可读来源时，后端测试覆盖升级为 resolved。
- 通过：只有内部 ID 时返回 unresolved / reason_code，不把内部 ID 当主 label。
- 通过：同 external_id 后续可升级，不同 unresolved 不合并。
- 通过：前台保留 `readableGroupDisplayLabel()` / unresolved / `internal_identifier_only` 保护；群管理、消息筛选、消息行、候选 / 日报关联 label 不回退内部 ID。

## 前台消息明细复核

- 通过：前台 `messageReadablePreview()` 优先消费 `content_text`、`content_preview`、`message_text`、`message_preview`、`text_preview`、`preview`、`summary`。
- 通过：前台 `messagePreviewLine()` 无正文时显示明确空态，不再用消息编号冒充摘要。
- 通过：`message_ref` 只保留为详情定位 / 技术定位字段。
- 通过：运行态后端已返回正文 / 预览字段，前台具备展示正文预览的数据基础。

## 回归复核

- 通过：英文非群误判修复不回退。
- 通过：`all_wechat_groups` / unresolved 去重 / 群名升级合同不回退。
- 通过：`config-center` forbidden 字段白名单不回退。
- 通过：`real_read_enabled=false` 默认口径未见回退。

## 安全边界

- 本轮未执行真实读取。
- 本轮未执行真实 roster 同步。
- 本轮未执行 `wx history/search/export/new-messages`。
- 本轮未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 本地 Windows UI payload 允许返回真实群名和真实消息正文 / 可读摘要；测试审查状态卡、回报队列、总控消息和 smoke 回传仍只写 count/status/error_code、字段存在性、布尔结果和日志路径，不摘录真实群名、真实消息正文、成员、客户名单、wxid/key/salt、DB path、IP、daemon 日志或 Windows 敏感路径。

## 需返工事项

- 无阻塞返工项。

## 剩余风险

- 本轮为 Mac 本机 8765 运行态复验；Windows 仍需按发布规则从 Git 发布目录拉取、重启服务并做字段级 smoke。
- 若 Windows 发布目录未同步或服务未重启，页面仍可能显示旧无正文空态。

## 下一棒建议

- 监工可收下本轮 P0 复验。
- 下一步进入发布前收口：提交 / 推送后由 Windows Git 发布目录拉取，重启 8765，并做 `/api/messages/v1` 字段级 smoke 与页面强刷验证。
- 真实读取、roster、自动外发 / 回复、正式区写入仍需单独授权。
