# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-20 20:17 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不读取真实微信消息正文；不执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`监工派工：2026-05-20｜测试审查｜config-center 字段白名单收紧复验`
- 复验对象：`监工返工：2026-05-20｜后端API｜config-center 字段白名单收紧`
- 结论：完成通过。`/api/config-center` full payload 已收紧，运行态字段扫描未再命中 DB path / sqlite path / 成员名单级字段；长期真实读取授权实战版主路径的默认关闭、persistent 状态、blocked 不执行真实读取、手动 / 定时 / 暂停 / 多白名单群 / 去重 / 本地链路测试证据未回退。
- 回传状态：send_input 不可用，已走回报队列。

## 审查范围

- 后端/API状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 监工回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_persistent_real_read_contract.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_daily_center_monitor_groups.py`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.example.yaml`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.windows.example.yaml`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.yaml`
- 证据日志：`.codex_py_logs/py-run-20260520-200831.log`、`.codex_py_logs/py-run-20260520-200841.log`、`.codex_py_logs/py-run-20260520-200913.log`、`.codex_runtime_logs/http-smoke-config-center-whitelist-20260520-201019.log`
- 运行态：127.0.0.1:8765 的 `/api/config-center`、`/api/status`、`/api/real-trial/run`

## 测试 / 证据

- 后端/API自证：定向白名单 `.codex_py_logs/py-run-20260520-200831.log`，5 selected passed。
- 后端/API自证：相关后端专项 `.codex_py_logs/py-run-20260520-200841.log`，75 passed。
- 后端/API自证：全量 pytest `.codex_py_logs/py-run-20260520-200913.log`，132 passed。
- 后端/API自证：运行态 smoke `.codex_runtime_logs/http-smoke-config-center-whitelist-20260520-201019.log`，HTTP 200，forbidden field hit count=0。
- 本轮复核：`git diff --check` exit=0。
- 本轮运行态 `/api/config-center` full payload：HTTP 200，session count=4，`path_fields_returned=false`，`member_list_returned=false`，forbidden hit count=0。
- 本轮运行态 `/api/status`：HTTP 200，persistent authorization 字段存在，`real_read_enabled=false`，forbidden hit count=0。
- 本轮运行态 `/api/real-trial/run` persistent 默认关闭：HTTP 200，`status=blocked`，`error_code=persistent_real_read_disabled`，`will_run=false`，`execution.no_real_read_executed=true`，`will_execute_wx_history=false`，`real_read_enabled_after=false`。
- 本轮未运行 Python；未执行真实读取、真实 roster 同步或 forbidden wx 命令。

## config-center 字段白名单复核

- 通过：`status.latest_trial` 已改为 pathless 摘要，仅保留 status/count/布尔/read_shape 类字段；不再返回 `sqlite_path`、`db_path`、`database_path`。
- 通过：`editable.sessions[].member_options` 已改为 count/status 摘要；运行态首个 session 的成员摘要字段只包含 scope、complete、count、available_count、appeared_count、roster_count、refresh_status、roster_status、full_sync 状态类字段。
- 通过：`/api/config-center` full payload 运行态扫描未命中 `member_name_options`、`member_options.names/items`、`appeared_members`、`roster_members`、`full_members`、`raw_payload`、DB path、wxid/key/salt/daemon 类字段。
- 通过：测试已补 full payload key-path 扫描，覆盖先前阻塞的 `status.latest_trial.sqlite_path` 和 `editable.sessions[].member_name_options` 等路径。

## Persistent 主路径回归复核

- 通过：长期授权契约仍为 `authorization_mode=persistent`，默认关闭。
- 通过：`/api/status`、`/api/config-center` 均保留 persistent authorization 摘要。
- 通过：`/api/real-trial/run` 在 persistent 默认关闭时 blocked，不执行真实读取。
- 通过：后端相关测试仍覆盖手动触发、定时触发开关、暂停 blocked、窗口上限、多启用白名单群、本地 raw/normalized/candidate 链路和重复运行去重。
- 通过：`real_read_enabled=false` 在 `config/app.yaml`、`config/app.example.yaml`、`config/app.windows.example.yaml` 中保持。

## 字段白名单 / 安全边界

- 本轮未执行真实读取、未执行真实 roster 同步、未执行 `wx history/search/export/new-messages`。
- 未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 状态卡、回报队列和运行态复核只记录 HTTP status、count/status、error_code、字段存在性、布尔结果和日志路径。
- 未摘录真实消息正文、候选正文、草稿正文、真实群名、真实会话、真实成员、客户名单、wxid/key/salt、DB 路径、IP、daemon 原始日志或 Windows 敏感路径。

## 需返工事项

- 无阻塞返工项。

## 剩余风险

- 前台若仍依赖 `/api/config-center.editable.sessions[].member_name_options` 作为成员下拉数据源，需要后续使用 `/api/monitor-groups/{group_id}` 或 refresh/sync 成员接口；这不阻塞本轮字段白名单复验。
- Windows 仍需拉取包含本棒收紧的新代码后再复验；Mac 本机通过不等于 Windows 实机通过。
- 本轮只验证默认关闭与 fake / fixture 契约，未执行真实长期读取。

## 下一棒建议

- 监工可收下本轮后端/API字段白名单返工。
- 下一步先做发布收口，确保本机已验收改动提交 / 推送并由 Windows Git 发布目录拉取，再做 Windows 运行态字段白名单与 persistent 默认关闭 smoke。
- Windows 侧真实长期授权开启和首跑仍需用户明确确认，只回传 count/status/error_code 摘要。
