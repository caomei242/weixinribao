# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-18 18:06 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不读取真实微信消息正文；不外发客户原文；不调用外部 LLM；不自动回复；不写正式待办池

## 本轮任务

- 任务标记：`监工派工：2026-05-18｜测试审查｜V1 P1 候选池统一与人话主流程联动复核`
- 复核对象：前台/UI助手 `监工派工：2026-05-18｜前台UI｜V1 P1 候选池统一与人话主流程`；后端/API助手 `监工派工：2026-05-18｜后端API｜V1 P1 候选池统一与人话文案数据口径`
- 结论：完成通过，可收下。P0 固定应用壳未回退；候选收件箱前台不再暴露两套来源切换；运行态 `/api/inbox/v1` 统一候选池返回 3 条候选且 `requires_source_switch=false`；草稿日报和转述摘要默认跟随统一候选池，未出现“候选 3 条但预览像 0 条”的断裂。
- 回传状态：当前工具列表未提供 `send_input`，send_input 不可用，已走回报队列；未取得 `submission_id`。

## 审查范围

- 项目材料：`/Users/gd/Desktop/微信agent专项/开发/状态卡/前台UI助手.md`
- 项目材料：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 项目材料：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 前台文件：`src/wechat_feedback_app/static/index.html`、`src/wechat_feedback_app/static/styles.css`、`src/wechat_feedback_app/static/app.js`
- 后端/API文件：`src/wechat_feedback_app/routes.py`
- 测试文件：`tests/test_real_trial_latest_visibility.py`、`tests/test_exporter.py`
- 证据日志：`.codex_py_logs/py-run-20260518-175120.log`、`.codex_py_logs/py-run-20260518-175804.log`、`.codex_py_logs/py-run-20260518-175951.log`、`.codex_py_logs/py-run-20260518-175958.log`、`.codex_runtime_logs/http-smoke-20260518-180050.log`
- 本轮只做静态源码、测试日志摘要、HTTP / DOM / count-status 级 smoke；未截图真实内容，未打开真实 SQLite / exports 正文，未摘录真实消息、候选或草稿正文。

## 测试 / 证据

- 后端/API TDD 红灯：`.codex_py_logs/py-run-20260518-175120.log` 存在，状态卡说明新增目标测试按预期失败。
- 后端/API TDD 绿灯：`.codex_py_logs/py-run-20260518-175804.log`，2 passed。
- 相关测试：`.codex_py_logs/py-run-20260518-175951.log`，40 passed。
- 全量 pytest：`.codex_py_logs/py-run-20260518-175958.log`，65 passed。
- 运行态 smoke：`.codex_runtime_logs/http-smoke-20260518-180050.log`，`/api/inbox/v1` HTTP 200、`candidate_count=3`、`requires_source_switch=false`、人话字段开发词命中 0；转述摘要预览 HTTP 200、默认 `data_source=real_trial`、`item_count=3`、`human_task` 开发词命中 0。
- 本轮复跑：`node --check src/wechat_feedback_app/static/app.js` exit=0；`git diff --check` exit=0。
- 本轮 HTTP 复验：`/`、`/api/inbox/v1`、`/api/export/templates/preview` 默认来源、`/api/daily-control/draft-preview` 默认来源、`/api/real-trial/latest` 均 HTTP 200。
- 本轮运行态结果：`candidate_inbox.count=3`、`candidate_inbox.requires_source_switch=false`、`candidate_inbox.items.length=3`；转述摘要默认 `item_count=3`；草稿预览默认 `candidate_count=3`。
- 本轮未运行 Python 命令；未执行真实读取类命令。

## P1 候选池统一复核

- 后端/API：`/api/inbox/v1` 新增 `candidate_inbox`，对外字段为 `count / summary_label / source_hint / requires_source_switch / primary_action / items` 等人话字段；运行态无 `raw_items`、无 `source_key`。
- 后端/API：主池为空且最近试读有候选时，统一候选池返回 3 条候选，`requires_source_switch=false`，不要求用户选择 `workspace / real_trial`。
- 前台/UI：候选收件箱已移除 `workspaceSourceBtn / realTrialSourceBtn` 两个来源切换控件。
- 前台/UI：候选页会同时读取今天处理区候选和最近试读候选，自动展示当前最该处理的一池；当前场景下可默认展示 3 条候选。
- 来源说明：内部来源最多显示为小标签和提示，例如“今天处理区 / 最近试读候选”，不再作为用户必须先选择的数据源。

## 人话文案复核

- 静态 HTML 可见文本开发词扫描：`workspace / real_trial / pending / none / tech / ops / resolved / real_read_disabled / formal_write` 命中 0。
- 运行态 `candidate_inbox`、`transfer_task`、转述摘要 `human_task` 人话字段开发词命中 0。
- 前台主要标题、按钮、空状态和任务说明使用“候选收件箱 / 今天处理区 / 最近试读候选 / 本机摘要 / 正式日报 / 正式待办”等用户语言。
- 代码仍保留 `workspace / real_trial / pending / tech / ops / none` 等内部枚举值和表单 value，但未作为主视觉文案或用户动作词暴露。

## 转述摘要用户任务化复核

- 前台转述摘要页有三类任务卡：`给内部产品或技术同步`、`给客户回复`、`给自己留今日复盘`。
- 后端/API `human_task` 返回 `task_title / task_subtitle / candidate_count_label / copy_ready_lines / task_options / primary_action_label` 等人话字段。
- 默认转述摘要预览跟随统一候选池：运行态默认来源为最近试读候选，`item_count=3`、`human_task.candidate_count=3`。
- 高级模板 / 本机文件仍在折叠或次级区域，不再抢主视觉。

## P0 应用壳回归复核

- 首屏仍为 `微信反馈防漏收件箱 V1`。
- `appShell=1`、`mainPanel=1`；左侧导航 7 个目标仍为 `today/messages/candidates/group-tags/draft/transfer/config`。
- `setWorkspacePage`、`pageShouldBeVisible`、`document.body.dataset.activePage` 仍存在；右侧唯一主工作区随导航切换。
- 非今日防漏页继续由 `workspace-page` / `active-page` / `body[data-active-page]` 控制，不回退成长页堆叠。

## 字段白名单 / 安全边界

- 核心复验接口 `/api/inbox/v1`、转述摘要预览、草稿预览的禁用值路径命中 0；禁用键路径命中 0。
- `/api/inbox/v1` 的安全口径显示 `default_real_read_enabled=false`、`formal_write_enabled=false`；草稿预览和转述摘要预览均显示正式写入禁用。
- `config/app.yaml` 与 `config/app.example.yaml` 均保持 `real_read_enabled: false`。
- 本轮未执行 `wx history`、`wx search`、`wx export`、`wx new-messages`。
- 本轮未运行真实 wx-cli，未读取真实消息正文，未打开真实 SQLite / exports 正文。
- 未发现正式待办池、正式日报、Obsidian 正式区或外部系统写入；未调用外部 LLM，未自动回复。
- 状态卡和回报队列只写 DOM 计数、HTTP status、count/status 和日志路径，不写真实正文、候选正文、草稿正文、wxid 具体值、真实会话列表、key、salt、真实 DB 路径或 daemon 原文。

## 需返工事项

- 无阻塞返工事项。

## 剩余风险

- 本轮仍未混入草莓客户管理系统只读匹配、全局人员库、P2 操作历史；这些应保持后续独立授权任务。
- 前台仍有少量内部枚举值存在于 JS 状态、API 参数和表单 value 中；当前不作为主视觉文案展示，后续若做更严格的 UI 文案审计，可再收紧。
- `/api/real-trial/latest` 仍承担试读产物状态接口；本轮只用它核对 count/status/default flag，未摘录产物路径或正文。
- 仓库存在既有 untracked 文件，本轮只读复核未执行 `git add` / `git commit`。

## 下一棒建议

- 监工可收下 V1 P1 候选池统一与人话主流程联动复核，通知用户刷新前台试 P1：进入候选收件箱应直接看到 3 条候选，进入转述摘要应看到三类用户任务且候选数一致。
- 下一棒建议单独派产品助手或用户做只读实操复验；若继续开发，建议进入 P1 后续小块：转述摘要细节体验、草莓客户系统只读匹配或全局人员库，但不要混入真实读取扩大范围。
