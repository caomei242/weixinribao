# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-21 12:16 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不手工执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`2026-05-21｜测试审查｜persistent all_wechat_groups history target 修复复验`
- 任务来源：监工派工
- 当前状态：完成通过
- 当前结论：后端/API P0 target 修复可收下。代码已避免把 `detected-wechat-group-*` / `local-monitor-*` 作为 wx history target；fake token-only 群会使用底层 token 调 history；本地 detected external_id 仍作为配置 / 展示 / 关联键；无效 target 会安全分类为 `session_identifier_mismatch` 并计入 `invalid_history_target_count`。可进入提交 / 推送，Windows 拉新版后复跑 persistent all_wechat_groups。
- 回传状态：send_input 不在当前工具列表；已按固定协议写入监工回报队列。

## 审查范围

- `/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- `/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- `src/wechat_feedback_app/config.py`
- `src/wechat_feedback_app/routes.py`
- `tests/test_persistent_real_read_contract.py`
- `git status --short --branch`
- 后端测试日志与 `git diff --check`

## 测试 / 证据

- 后端状态卡：2026-05-21 12:14 CST，任务 `persistent all_wechat_groups history target 修复` 完成。
- persistent 专项：`.codex_py_logs/py-run-20260521-121331.log`，25 passed。
- 相关后端专项：`.codex_py_logs/py-run-20260521-121338.log`，116 passed。
- 全量 pytest：`.codex_py_logs/py-run-20260521-121346.log`，157 passed。
- `git diff --check`：exit=0。
- 工作区状态：本轮后端相关文件 `config.py`、`routes.py`、`tests/test_persistent_real_read_contract.py` 有改动；同时仍存在前台/UI相关脏改和调度材料，提交 / 发布前需要按范围区分。

## history target 修复复核

- `SessionConfig` 新增 `history_target`、`wx_session_token`、`source_session_id`。
- 探针从 `history_target / wx_session_token / source_session_id / id / external_id / username / room_id / chat_id / conversation_id / session_id` 等字段提取底层 history target。
- wx history target 选择顺序为 `history_target / wx_session_token / source_session_id` > resolved 可读 display name > external_id。
- 执行前检查无效 target；若最终 target 是 `detected-wechat-group-*` / `local-monitor-*`，不调用 wx history，直接分类失败。
- 结论：代码层已避免本地 hash 进入 wx history target。

## fake token-only 场景复核

- 测试覆盖 fake wx sessions 只有底层 token、无可读名的群。
- 断言 history 命令使用底层 token，不使用 `detected-wechat-group-*`。
- 断言本地 `SessionConfig.external_id` 仍保留 detected hash；`history_target / wx_session_token / source_session_id` 保存执行 target。
- 断言结果 payload 不泄露该 token 字符串。
- 结论：真实读取可用性与本地关联键稳定性兼顾。

## 本地关联链路复核

- 本地配置 / 展示 / 关联键继续使用 detected external_id，不直接把 external_id 改为真实 token。
- `upsert_detected_monitor_groups` 会为已存在 detected 群补齐缺失 target；同 external_id 后续可升级。
- raw / normalized / candidate 链路仍可使用本地 session 关联；执行 target 只作为 wx history 调用参数。
- 结论：未发现破坏本地链路的证据。

## failure classification 复核

- 无效 history target 会返回 `status=failed`、`error_code=real_trial_history_failed`。
- `invalid_history_target_count` 计数存在。
- `history_failure_classification=session_identifier_mismatch` 与 `history_failure_category_counts.session_identifier_mismatch` 可用。
- `details_returned=false` / `history_failure_details_returned=false` 保留，不透传 raw stdout/stderr、命令明细或真实 token。
- 结论：分类可用于 Windows 下一轮判断是否仍卡在 target mismatch。

## 字段白名单 / 安全边界

- `/api/config-center` 公共 payload 走 `config_center_session_payload`，未见 `history_target / wx_session_token / source_session_id` 暴露入口；测试断言 forbidden 字段不出现。
- `write_config_center_yaml` 可将执行 target 写入本地配置文件，这是本地执行链路需要；真实配置仍不得入 git。
- 状态卡、回报队列、测试日志和结果 payload 均通过敏感字段断言，不摘录真实 token、wxid、真实群名、正文、成员、客户名单、DB path、IP 或 daemon 原文。
- 本轮未执行真实读取；未执行 roster；未手工执行 `wx history/search/export/new-messages`；未写正式日报、正式待办池、Obsidian 正式区或外部系统。

## 不回退项

- persistent 默认关闭：测试覆盖，`real_read_enabled=false` 事后保持。
- all_wechat_groups 范围：fake executor 覆盖，旧 scope 不回退。
- 探针失败 blocked 不调用 executor：既有 persistent 合同覆盖。
- failure classification：保留上一棒分类字段。
- 英文 `room/group` 非群误判防线：相关后端专项覆盖。
- 字段白名单：相关后端专项和本轮敏感字段断言覆盖。

## 需返工事项

- 本轮无新的后端/API返工事项。
- 发布收口提醒：当前工作区混有前台/UI和阶段材料脏改；如果监工要做 P0 发布闸门，建议只纳入后端 target 修复相关文件，或先明确一并发布范围。

## 剩余风险

- 本轮未在 Windows 执行真实读取；只有代码 / 测试层验证。
- Windows 拉新版后仍需复跑 persistent all_wechat_groups，确认 failure category 不再是 `session_identifier_mismatch`，并观察 `sessions_success / raw_messages_seen / raw_messages_inserted / candidate_items_created` 是否转正。
- 若仍失败，应按新的 failure category 继续定位 wx-cli 参数、权限 / DB / 连接、时间窗口或数据解析问题。

## 下一棒建议

- 监工可进入提交 / 推送；Windows 拉新版后复跑 persistent all_wechat_groups。
- Windows 回传只写 `sessions_success`、`raw_messages_seen`、`raw_messages_inserted`、`candidate_items_created`、`failure_category`、`invalid_history_target_count`、count/status/error_code，不贴真实 token、真实群名或正文。
