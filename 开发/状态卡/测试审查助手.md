# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-21 12:36 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不手工执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`2026-05-21｜测试审查｜群名人工命名入口 P0 复验`
- 任务来源：监工派工
- 当前状态：完成通过
- 当前结论：群名人工命名入口 P0 可收下。前台已清晰提供“本地显示名 / 监控群名称”入口并发送 `group_name/display_name`；后端支持 `group_name/display_name` 与多种本地别名字段，保存后返回 `resolved/user_input` 和 count/readback 摘要；列表、详情、消息筛选、候选/日报 label 读回路径有测试覆盖；不同 unresolved 不按占位名误合并。
- 回传状态：send_input 不在当前工具列表；已按固定协议写入监工回报队列。

## 审查范围

- `/Users/gd/Desktop/微信agent专项/开发/状态卡/前台UI助手.md`
- `/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- `/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- `src/wechat_feedback_app/static/index.html`
- `src/wechat_feedback_app/static/app.js`
- `src/wechat_feedback_app/routes.py`
- `src/wechat_feedback_app/daily_control.py`
- `tests/test_local_ui_display_contract.py`
- `git status --short --branch`
- 前后端测试日志、`node --check`、`git diff --check`

## 测试 / 证据

- 前台/UI状态卡：2026-05-21 11:59 CST，`Windows 6 个 unresolved 群名人工命名入口预案` 完成。
- 前台证据：`node --check src/wechat_feedback_app/static/app.js` exit=0；静态 smoke 关键布尔均为 true；`git diff --check` exit=0。
- 后端/API状态卡：2026-05-21 12:33 CST，`群名人工命名保存读回确认/补契约` 完成。
- 后端测试：`.codex_py_logs/py-run-20260521-123305.log`，本地 UI 合同专项 12 passed。
- 后端测试：`.codex_py_logs/py-run-20260521-123312.log`，相关后端专项 117 passed。
- 后端测试：`.codex_py_logs/py-run-20260521-123321.log`，全量 pytest 158 passed。
- 复核命令：`node --check src/wechat_feedback_app/static/app.js && git diff --check` exit=0。
- 当前工作区：`routes.py`、`daily_control.py`、`static/app.js`、`static/index.html`、`tests/test_local_ui_display_contract.py` 以及状态卡 / 回报队列有改动；另有未跟踪调度材料，提交前需明确发布范围。

## UI 人工命名入口复核

- `index.html` 中字段标签为 `本地显示名 / 监控群名称`。
- 存在 `monitorGroupDisplayName` 输入框、`monitorGroupDisplayNameHint` 提示、`saveMonitorGroupBtn` 保存按钮。
- unresolved / 无可读名时，前台提示用户手动填写本地显示名以替换 `群名待解析`。
- 有可读名时，提示保存后用于左侧列表、消息筛选和候选 / 日报关联。
- 结论：入口对用户足够明确，不再只是隐藏在普通“监控群名称”字段里。

## 保存 payload 复核

- `readMonitorGroupForm()` 从 `monitorGroupDisplayName` 读取本地显示名。
- `monitorGroupToPayload()` 提交 `group_name` 与 `display_name`。
- 保存接口对已存在群使用 `/api/monitor-groups/{group_id}`，新建群使用 `/api/monitor-groups`。
- 保存成功后会重新读取群详情并更新本地列表状态。
- 结论：前台 payload 与后端兼容契约对齐。

## 后端保存读回复核

- 后端继续支持 `group_name/display_name`。
- 新增兼容字段：`manual_display_name`、`local_display_name`、`local_alias`、`display_alias`、`alias`。
- 保存人工名后：`display_name_status=resolved`，`display_name_source=user_input`。
- 保存响应返回：`manual_display_name_saved`、`display_name_readback_status`、`display_name_readback_source`、`readable_group_label_count`、`unresolved_group_label_count`、`readback_contract`。
- 结论：后端读回合同完整，可用于 Windows 保存前后 count/status smoke。

## 读回覆盖复核

- 列表：`/api/monitor-groups` 的 `group_name/display_name` 可返回人工名。
- 详情：`/api/monitor-groups/{group_id}` 的 `group.group_name` 可读回。
- 消息筛选：`message_group_options[].group_name` 可读回。
- 候选 / 日报：`candidate_inbox`、`daily_center.today_focus`、`daily_followup_items_payload`、`daily_center_today_focus_payload`、`build_candidate_inbox_items` 通过 `group_label` 读回。
- 测试覆盖目标项从 unresolved 变 resolved，另一 unresolved 保持不变。
- 结论：不同 unresolved 不按占位名误合并，只更新指定 group_id。

## 字段白名单 / 安全边界

- 状态卡、回报队列、测试日志和本复验均只记录 count/status、字段存在性、布尔结果、日志路径；未摘录真实人工名、真实群名、正文、token、wxid、成员、客户名单、DB path、IP 或 daemon 日志。
- 本轮未执行新的真实读取；未执行 roster；未手工执行 `wx history/search/export/new-messages`；未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- `real_read_enabled=false` 事后保持由后端状态卡和相关测试覆盖。

## 不回退项

- 真实读取 history target 修复：上一棒已验收；本轮相关后端专项继续覆盖 persistent 合同。
- 消息正文可读：本地 UI 合同专项继续覆盖。
- persistent 默认关闭 / 事后关闭：相关后端专项覆盖。
- all_wechat_groups 范围：相关后端专项覆盖。
- 英文 `room/group` 非群误判：相关后端专项覆盖。
- `真实首跑入库候选 0 补证`：仍未收口，但不作为本 P0 人工命名入口阻塞。

## 需返工事项

- 本轮无新的前台/UI或后端/API返工事项。

## 剩余风险

- 本轮未在 Windows 运行态实际保存 6 个 unresolved 群名；需要发布后先选 1 个 unresolved 群做保存读回 smoke。
- 如果 Windows 运行态仍是旧后端，或前台请求未命中指定 `group_id`，可能出现保存后仍显示 `群名待解析`；需用保存前后 count/status 和页面布尔定位。

## 下一棒建议

- 可以进入提交 / 推送；Windows 拉新版后先对 1 个 unresolved 群保存人工名。
- Windows 回传只写保存前后 `readable_group_label_count`、`unresolved_group_label_count`、目标项 `display_name_status/group_name_status`、列表 / 详情 / 消息筛选 / 候选或日报 label 是否 resolved 的布尔结果。
- 单个通过后，再批量处理剩余 unresolved 群；随后回接 `真实首跑入库候选 0 最小补证`。
