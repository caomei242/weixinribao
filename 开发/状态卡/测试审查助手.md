# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-20 18:14 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不读取真实微信消息正文；不执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`监工派工：2026-05-20｜测试审查｜一次性真实试验授权入口与 Windows 配置上限契约复核`
- 结论：完成通过。后端/API 18:09 返工已补齐一次性执行入口、token/marker 授权门槛、Windows 配置上限字段与 blocked 原因枚举；运行态 smoke 仅验证 dry-run / blocked 摘要，未调用 token 齐全真实执行路径。
- 回传状态：已发监工：019e44e2-1f0d-7471-adea-77eaf27a5d6f；同时已按固定协议追加监工回报队列。

## 审查范围

- 状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/测试审查助手.md`
- 回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 配置样例：`/Users/gd/Desktop/微信agent专项/config/app.example.yaml`
- 配置样例：`/Users/gd/Desktop/微信agent专项/config/app.windows.example.yaml`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/collector.py`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/config.py`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_expanded_real_trial_contract.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_real_connection_redaction.py`
- 证据日志：`.codex_py_logs/py-run-20260520-180758.log`、`.codex_py_logs/py-run-20260520-180807.log`、`.codex_py_logs/py-run-20260520-180815.log`、`.codex_runtime_logs/http-smoke-real-trial-once-contract-20260520-180858.log`
- 运行态：127.0.0.1:8765 的 `/api/status`、`/api/real-trial/run`

## 测试 / 证据

- `git status --short --branch`：工作区包含后端/API本棒相关改动和历史状态卡 / 回报队列改动；测试审查未改业务代码。
- 后端/API 自证：一次性授权执行入口专项 `.codex_py_logs/py-run-20260520-180758.log`，15 passed；相关后端专项 `.codex_py_logs/py-run-20260520-180807.log`，72 passed；全量 pytest `.codex_py_logs/py-run-20260520-180815.log`，121 passed；运行态 smoke `.codex_runtime_logs/http-smoke-real-trial-once-contract-20260520-180858.log`，node exit=0。
- 本轮复核：`git diff --check` exit=0。
- 本轮运行态 `/api/status`：HTTP 200，`real_trial.enabled=false`，`default_real_read_enabled=false`，`scope_mode=configurable_window`，`max_allowed_lookback_days=30`，`execute_once_field=execute_once`，`one_time_authorization_token_required=true`，敏感命中 false。
- 本轮运行态 dry-run 未开执行入口：HTTP 200，`status=dry_run_ready`，`error_code=real_trial_execution_entry_not_opened`，`will_run=false`，`entry_opened=false`，`no_real_read_executed=true`，窗口字段存在，敏感命中 false。
- 本轮运行态请求执行但缺 token：HTTP 200，`status=blocked`，`error_code=one_time_authorization_token_required`，`will_run=false`，`real_read_enabled_after=false`，窗口字段存在，敏感命中 false。
- 本轮未运行 Python；未执行真实读取、真实 roster 同步或 forbidden wx 命令。

## 一次性授权入口复核

- 通过：执行入口字段支持 `execute_once=true` / `run_once=true` / `execute_real_trial_once=true` / `open_execution_path=true`。
- 通过：一次性授权字段支持 `one_time_authorization_token` / `authorization_token` / `one_time_token`，也支持 `one_time_authorization_marker=true` / `one_time_authorization_confirmed=true` / `authorization_marker=true`。
- 通过：仍要求 `confirmed=true`、`authorize_expanded_real_read_trial=true`、`test_wechat_account_confirmed=true`、`one_time_expanded_trial=true`，以及合法群范围、消息上限和批次上限。
- 通过：未开执行入口只返回 dry-run 摘要；开执行入口但缺 token / marker 会 blocked；token 齐全执行路径只在 fake executor 测试中验证，本轮没有调用真实路径。

## Windows 配置上限契约复核

- 通过：`wx_cli.expanded_real_lookback_days` 已改为 float 并控制 `max_allowed_lookback_days`。
- 通过：`config/app.example.yaml` 与 `config/app.windows.example.yaml` 均包含 `expanded_real_lookback_days` 和群 / 消息 / 批次上限字段，默认 `real_read_enabled=false`。
- 通过：测试覆盖旧 2 小时上限约 `0.0833` 天时，30 天请求返回 `expanded_trial_lookback_days_too_large` 和 `limit_reason=exceeds_configured_lookback`。
- 通过：30 天不是写死边界；需要 30 / 60 / 90 天时通过运行配置或显式授权上限切换。

## Blocked 原因复核

- 通过：缺扩大试验授权、缺测试微信号确认、缺一次性试验声明、缺一次性 token / marker、窗口超过配置上限、窗口参数非法、群范围不合法、消息 / 批次上限不合法均有独立 error_code / reason_code。
- 通过：运行态缺 token blocked 与未开执行入口 dry-run 可区分。

## Fake 执行路径 / 失败摘要复核

- 通过：测试覆盖配置上限 30 天 + 授权字段齐全 + token 进入 fake 执行路径，返回 count/status 级 `execution_summary`。
- 通过：测试覆盖 fake 执行失败摘要，包含 sessions / raw / candidate 计数，不返回正文或名单。
- 通过：测试覆盖执行后 `real_read_enabled=false` 事后保持。

## 字段白名单 / 安全边界

- 本轮只记录 HTTP status、count/status、error_code、字段存在性、布尔结果和日志路径。
- 未调用 token 齐全真实执行路径；未执行新的真实读取，未执行真实 roster 同步，未执行 `wx history/search/export/new-messages`。
- 未打开或摘录真实 SQLite / exports 正文。
- 未自动定时采集，未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 未记录真实消息正文、候选正文、草稿正文、真实群名、真实会话列表、真实成员名单、客户名单、wxid、key、salt、真实 DB 路径、IP、Windows 敏感路径或 daemon 原始日志。

## 需返工事项

- 无阻塞返工项。

## 剩余风险

- 本轮通过的是代码侧契约、fake executor 测试和 dry-run / blocked 运行态摘要；不代表已经授权或执行 Windows 真实读取。
- Windows 现场若仍显示 `max_allowed_lookback_days=0.0833`，需要先切换 Windows 运行配置到本次授权窗口，再走一次性授权 payload。
- 前台/UI 尚未提供可视化一次性授权按钮；当前真实执行需由监工 / 总控给出明确 payload 并由 Windows 现场按接口执行，不能手工绕过接口跑 wx 命令。

## 下一棒建议

- 可收下后端/API返工。
- 建议监工 / 总控准备 Windows 一次性真实试验授权 payload，明确 `execute_once`、一次性 token / marker、窗口配置上限、群范围、消息 / 批次上限和禁止项。
- Windows 执行前先复核 `/api/status` 的 `max_allowed_lookback_days` 已切到授权值，且 `real_read_enabled=false` 仍保持默认关闭。
