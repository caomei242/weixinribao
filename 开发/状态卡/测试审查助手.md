# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-20 23:29 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不读取真实微信消息正文；不执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`2026-05-20｜测试审查｜unresolved 群名占位去重防误合并复验`
- 复验对象：后端/API `unresolved 群名占位去重防误合并`
- 结论：完成通过。`upsert_detected_monitor_groups()` 已收紧名称去重：只有 resolved 可读群名参与 `by_name` 匹配；unresolved / 占位名 / internal_identifier_only 不参与跨 external_id 去重。不同 external_id 的 unresolved 群会分别新增，同一 external_id 后续拿到 resolved 可读名可升级。
- 回传状态：`send_input` 工具当前未暴露，已追加监工回报队列作为兜底完成回报。

## 审查范围

- 后端/API状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 监工回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_persistent_real_read_contract.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_local_ui_display_contract.py`
- 日志：`.codex_py_logs/py-run-20260520-232615.log`、`.codex_py_logs/py-run-20260520-232622.log`、`.codex_py_logs/py-run-20260520-232630.log`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.yaml`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.example.yaml`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.windows.example.yaml`

## 测试 / 证据

- 后端/API自证：最小相关 `.codex_py_logs/py-run-20260520-232615.log`，27 passed。
- 后端/API自证：相关后端专项 `.codex_py_logs/py-run-20260520-232622.log`，102 passed。
- 后端/API自证：全量 pytest `.codex_py_logs/py-run-20260520-232630.log`，148 passed。
- 后端/API自证：`git diff --check` 通过。
- 本轮复核：`git diff --check` exit=0。
- 本轮复核：`config/app.yaml`、`config/app.example.yaml`、`config/app.windows.example.yaml` 均保持 `real_read_enabled=false`；样例配置中 persistent 默认关闭。
- 本轮未执行真实读取、真实 roster 同步或 forbidden wx 命令；未打开真实 SQLite / exports 正文。

## unresolved 去重防误合并复核

- 通过：`by_name` 只索引 `local_group_display_meta(...).status == resolved` 且 `display_name_status != unresolved` 的已有 session。
- 通过：待检测 session 也只有在 detected meta 为 resolved 时才允许走 name match；unresolved / 占位名不会用 `by_name` 跨 external_id 匹配。
- 通过：同一 external_id 仍优先按 id 匹配，保持幂等。
- 通过：已有 unresolved 同一 external_id 后续拿到 resolved 可读名时，会升级 display name / display_name_status / source / reason_code。
- 通过：不同 external_id 的 unresolved 群会分别新增，不会因为统一占位文案被合并。

## 回归复核

- 通过：`all_wechat_groups` fake scope、可读群名 / internal-only unresolved 合同不回退。
- 通过：英文 `room/group` 非群误判修复不回退。
- 通过：本地主显示字段内部 ID 兜底不回退；内部 ID 不再作为主标题 / 主 label。
- 通过：safe/report/redacted 字段仍用于跨线程回报、smoke、测试审查和 report-safe 文本。
- 通过：`/api/config-center` forbidden field hit count 合同不回退，DB path / sqlite path / member list / raw payload 类禁字段仍不得返回。

## 安全边界

- 通过：未执行真实消息读取。
- 通过：未执行真实 roster 同步。
- 通过：未执行 `wx history/search/export/new-messages`。
- 通过：未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 通过：状态卡和回报只记录 count/status/error_code、字段存在性、布尔结果和日志路径；未摘录真实群名、真实 id、消息正文、候选正文、草稿正文、真实会话、成员名单、客户名单、wxid/key/salt、DB path、IP、daemon 原始日志或 Windows 敏感路径。

## 需返工事项

- 无阻塞返工项。

## 剩余风险

- 本轮为发布前后端小修复验，未做 Windows 运行态重载 / HTTP smoke。
- Windows 发布后仍需确认：多个待解析群能以不同本地监控群记录存在，页面主显示为“群名待解析 / 待补群名”，不显示内部 ID。
- 若后续上游探针提供可读群名，同 external_id 应升级为可读群名；真实可读群名仍依赖上游字段。

## 下一棒建议

- 监工可收下本小修，合并进同一批发布。
- 发布 / Windows 拉取后做 count/status 级复验：确认 release head、服务来源、8765 重启 / 静态刷新；页面只接受“可读群名”或“群名待解析 / 待补群名”，不能接受内部 ID。
- 真实读取 / roster / 正式区写入仍需单独授权。
