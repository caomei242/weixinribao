# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-20 22:46 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不读取真实微信消息正文；不执行真实读取 / roster 同步 / `wx history/search/export/new-messages`；不外发客户原文；不调用外部 LLM；不自动回复；不写正式日报、正式待办池、Obsidian 正式区或外部系统

## 本轮任务

- 任务标记：`监工派工：2026-05-20｜测试审查｜本地操作台主显示字段去误脱敏复验`
- 复验对象：`监工返工：2026-05-20｜后端API｜本地操作台主显示字段去误脱敏`
- 结论：完成通过。后端已把本地 UI 主显示字段与 safe/report 字段分层：本地操作台主字段恢复可识别值，`*_safe` / `safe_*` / `redacted_*` 和日报 / 转述 / smoke / 跨线程回报仍走脱敏摘要；`/api/config-center` forbidden field hit count 未回退。
- 回传状态：`send_input` 工具当前未暴露，已追加监工回报队列作为兜底完成回报。

## 审查范围

- 后端/API状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 监工回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- 源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/exporter.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_local_ui_display_contract.py`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.yaml`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.example.yaml`
- 配置：`/Users/gd/Desktop/微信agent专项/config/app.windows.example.yaml`
- 日志：`.codex_py_logs/py-run-20260520-223701.log`、`.codex_py_logs/py-run-20260520-224013.log`、`.codex_py_logs/py-run-20260520-224022.log`
- 本轮补充禁字段计数日志：`.codex_py_logs/py-run-20260520-224549.log`

## 测试 / 证据

- 后端/API自证：新增专项 `.codex_py_logs/py-run-20260520-223701.log`，5 passed。
- 后端/API自证：相关后端专项 `.codex_py_logs/py-run-20260520-224013.log`，96 passed。
- 后端/API自证：全量 pytest `.codex_py_logs/py-run-20260520-224022.log`，142 passed。
- 监工补跑：`git diff --check` exit=0。
- 本轮复核：`git diff --check` exit=0。
- 本轮复核：`config/app.yaml`、`config/app.example.yaml`、`config/app.windows.example.yaml` 均保持 `real_read_enabled=false`；样例配置中 persistent 默认关闭。
- 本轮补充验证：`.codex_py_logs/py-run-20260520-224549.log`，Python exit=0，`config_center_forbidden_hit_count=0`，`real_read_enabled=False`。
- 说明：首次补充验证 `.codex_py_logs/py-run-20260520-224534.log` 因未设 `PYTHONPATH=src` 导入失败，未执行业务逻辑；已按静默规则重跑通过。
- 本轮未执行真实读取、真实 roster 同步或 forbidden wx 命令；未打开真实 SQLite / exports 正文。

## 本地 UI 主显示字段复核

- 通过：新增 `local_ui_display_text()` / `local_ui_display_list()`，用于本地 UI 主显示字段，保留可识别文本，同时仍拦截 wxid、key/salt、daemon、raw/content 标记和路径类硬禁内容。
- 通过：群管理 `group_name`、`display_name`、`customer_name`、`module_name`、`group_type`、`customer_stage`、`owner_label`、详情联系人 / 备注等主字段不再被通用脱敏覆盖。
- 通过：消息明细 `group_name`、`customer_label`、`module_label` 主字段可读，且 `content_returned=false`、`raw_payload_returned=false` 保持。
- 通过：我方人员 `person_name`、`wechat_display_name`、`modules`、`notes`、suggestion 主字段可读。
- 通过：候选 / 日报 / 最近试读候选 `title`、`summary`、`module_name`、`customer_label` 等本地审阅主字段可读。

## safe / report 字段脱敏复核

- 通过：群管理、消息明细、我方人员、候选 / 日报、最近试读候选均保留或新增 `*_safe` / `safe_*` / `redacted_*` 字段，测试断言这些字段继续出现脱敏标记。
- 通过：`redact_visible_text()` 继续覆盖 wxid、secret、手机号 / 长数字、key/salt/daemon/raw/content、Unix / Windows / 项目相对路径样式文本。
- 通过：日报全文、转述预览、smoke、跨线程回报用途仍应消费 safe/report 字段；本轮未摘录 Markdown 正文、候选正文或真实明细。
- 通过：最近试读候选 payload 不返回 `content_text`，也不返回本地绝对路径。

## config-center 字段白名单复核

- 通过：`config_center_payload()` 中 session 的 `member_options` 仍为 count/status 摘要，并提供详情 endpoint；不返回 `member_name_options`、`member_options.names/items`、`appeared_members`、`roster_members`、`full_members` 等名单级字段。
- 通过：补充禁字段计数验证显示 `/api/config-center` full payload 对 `sqlite_path`、DB path、`member_name_options`、raw payload 类禁字段命中为 0。
- 通过：已有测试继续覆盖 config-center full payload 不返回路径字段和成员名单字段。

## 字段白名单 / 安全边界

- 通过：状态卡、回报队列和测试日志只记录 count/status/error_code、字段存在性、布尔结果和日志路径，不摘录真实群名、真实消息正文、真实会话、真实成员、客户名单、wxid/key/salt、DB path、IP、daemon 原始日志或 Windows 敏感路径。
- 通过：`real_read_enabled=false` 保持；未新增真实读取。
- 通过：未执行真实 roster 同步或 `wx history/search/export/new-messages`。
- 通过：未写正式日报、正式待办池、Obsidian 正式区或外部系统。

## 需返工事项

- 无后端/API阻塞返工项。

## 剩余风险

- 本棒未改前台静态文件；如 Windows 页面仍消费 safe 字段或旧缓存，需要前台/UI最小返工改为消费主显示字段。
- 8765 / Windows 运行态仍需发布 / 重载后按本地页面验证：群管理、消息明细、我方人员、候选审阅页不再把主操作文本显示成脱敏占位。
- 本轮只做代码与测试合同复验，未读取真实页面内容或真实数据正文。

## 下一棒建议

- 监工可收下后端/API返工。
- 下一棒建议派前台/UI或 Windows 运行态轻量复验：确认页面实际消费主显示字段，而不是 safe 字段或旧缓存。
- Windows 发布前仍按项目规则核对 Git 发布目录、服务来源和 8765 运行态；真实读取 / roster / 正式区写入仍需单独授权。
