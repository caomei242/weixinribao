# 微信agent专项 测试审查助手状态卡

更新时间：2026-05-20 12:49 CST

## 线程身份

- 线程名：微信agent专项 测试审查助手
- 线程 id：019e2a96-9e31-7dd1-a2fe-fd42756ab929
- 负责范围：测试审查、验收、风险复核、证据整理、返工建议
- 明确边界：只读审查；不改业务代码；不改真实业务数据；不读取真实微信消息正文；不外发客户原文；不调用外部 LLM；不自动回复；不写正式待办池

## 本轮任务

- 任务标记：`监工派工：2026-05-20｜测试审查｜群管理归档删除与日报生成反馈 P0 复验`
- 结论：完成通过。后端归档 / 删除契约、统计读回、日报生成反馈字段已具备；前台归档 / 删除入口、二次确认、安全文案、失败保留旧列表和日报生成 1 秒内反馈已复验；Windows P0 readiness/messages/首屏布局未回退。
- 回传状态：当前工具列表未提供 `send_input`，已按固定协议追加监工回报队列。

## 审查范围

- 状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/后端API助手.md`
- 状态卡：`/Users/gd/Desktop/微信agent专项/开发/状态卡/前台UI助手.md`
- 回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`
- 后端源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/config.py`
- 后端源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- 前台源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/index.html`
- 前台源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/styles.css`
- 前台源码：`/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/app.js`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_windows_p0_backend.py`
- 测试：`/Users/gd/Desktop/微信agent专项/tests/test_real_trial_latest_visibility.py`
- 运行态：127.0.0.1:8765 的 `/api/monitor-groups`、`/api/daily-center/generation-status`、`/api/windows-readiness`、`/api/messages/v1`、`/api/daily-center`、`/api/internal-people`

## 测试 / 证据

- 后端/API 自证：相关专项 `.codex_py_logs/py-run-20260520-124114.log`，35 tests OK；全量 pytest `.codex_py_logs/py-run-20260520-124118.log`，101 passed；`git diff --check` exit=0；8765 重载日志 `.codex_runtime_logs/restart-8765-archive-delete-20260520-124132.log`；HTTP smoke `.codex_runtime_logs/http-smoke-archive-delete-20260520-124232.log`。
- 前台/UI 自证：`node --check`、静态文件 `git diff --check`、全局 `git diff --check`、静态 grep smoke、运行态 Chrome/CDP 拦截式 smoke 均为 exit=0。
- 本轮复跑：`node --check src/wechat_feedback_app/static/app.js` exit=0。
- 本轮复跑：`git diff --check` exit=0。
- 本轮运行态 GET smoke：`/api/monitor-groups` HTTP 200，total=4，active=4，archived=0，dailyCenter=1，archive/delete 动作字段存在，群级字段契约存在，待验证或归档群不计入日报统计，敏感模式命中 false。
- 本轮运行态 GET smoke：`/api/daily-center/generation-status` HTTP 200，`feedback_state=idle`，`running/success/failed/old_report_preserved` 字段存在，敏感模式命中 false。
- 本轮运行态 GET smoke：`/api/windows-readiness` HTTP 200，`realReadEnabled=false`；`/api/messages/v1` 全部群和单群 HTTP 200，单群 count=0 且 notFallbackToAll=true，敏感模式命中 false。
- 本轮前台拦截式 smoke：归档按钮存在、删除按钮存在、生成按钮存在；归档 / 删除确认均出现；安全文案命中；取消确认后显示旧列表不变；生成点击 250ms 内 postSeen=true、按钮 disabled=true、按钮进入生成中、meta 显示生成中、旧日报保留提示存在；拦截响应后按钮恢复、状态 success、成功反馈存在。
- 本轮未运行 Python。

## 归档 / 删除复核

- 通过：后端存在 `POST /api/monitor-groups/{group_id}/archive` 和 `POST /api/monitor-groups/{group_id}/delete`。
- 通过：删除接口要求 `confirm_delete=true` 或等价确认字段；未确认路径返回 `confirmation_required` 且不删除。
- 通过：归档会把监控群移出启用、每日监控和日报统计；归档群 `counts_in_daily_center=false`，待验证群也不计入日报监控群统计。
- 通过：`/api/monitor-groups` 返回 `active_count`、`archived_count`、`daily_center_count`、动作字段、`can_archive`、`can_delete`、`delete_requires_confirmation`、`archived`、`status_label`、`counts_in_daily_center`。
- 通过：前台群管理页有归档 / 删除入口；二次确认说明只影响本项目本地配置，不删除真实微信群、不外发、不写正式区；失败 / blocked 时恢复旧列表并给人话反馈。
- 说明：本轮未在真实运行态配置上执行确认归档或确认删除；mutation 由 fake/stub 单测和前台拦截式 smoke 复核，符合只读边界。

## 日报生成反馈复核

- 通过：后端 `/api/daily-center/generation-status` 返回 `feedback_state`、`running`、`success`、`failed`、`old_report_preserved`，生成失败路径保留旧日报并返回可重试错误摘要。
- 通过：前台点击生成后 250ms 内进入生成中，按钮禁用，日报 meta 显示生成中和旧日报保留提示。
- 通过：前台成功路径有明确成功反馈并恢复按钮；失败路径代码有“生成失败 / 旧日报已保留”的人话提示。
- 说明：本轮用 fetch 拦截做运行态点击复核，未触发真实日报生成写入。

## Windows P0 回归

- 通过：`/api/windows-readiness` 仍为前台 readiness 来源，未回退为浏览器平台硬判断。
- 通过：消息明细仍调用 `/api/messages/v1`，单群 0 条保持空态，不回退全部群。
- 通过：日报中心首屏布局未回退；静态资源版本更新为 `20260520-archive-delete-feedback`。
- 通过：日报中心、群管理、我方人员、消息明细、候选入口、日报生成状态接口均未见 P0 回退。

## 字段白名单 / 安全边界

- 本轮只记录 HTTP status、count/status、字段存在性、布尔结果、按钮存在性、确认文案存在性和日志路径。
- 未执行新的真实读取，未执行真实 roster 同步，未执行 `wx history/search/export/new-messages`。
- 未打开或摘录真实 SQLite / exports 正文。
- 未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 未记录真实消息正文、候选正文、草稿正文、真实会话列表、真实成员名单、客户名单、wxid 具体值、key、salt、真实 DB 路径或 daemon 原始日志。
- 配置与运行态均保持真实读取默认关闭。

## 需返工事项

- 无阻塞返工项。

## 剩余风险

- Windows 实机微信登录、wx-cli 能力、路径权限仍需实机部署复验。
- 本轮未在真实配置上执行确认归档 / 确认删除，避免破坏当前本地监控群配置；真实操作需用户在前台自行确认。
- 本轮未执行真实日报生成写入，点击反馈通过前台拦截式 smoke 验证。

## 下一棒建议

- 可通知用户刷新前台并进入 Windows 实机复验。
- Windows 实机复验继续只记录 count/status/source/error_code 和字段存在性，不摘录真实正文、名单或路径。
