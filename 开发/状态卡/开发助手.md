# 微信agent专项 开发助手状态卡

更新时间：2026-05-18 16:13 CST

## 线程身份

- 线程名：微信agent专项 开发助手
- 线程 id：019e2a80-0aaa-7ab3-bda0-d7f8e53da989
- 负责范围：本项目实现开发、fixture / real 安全边界、本地 wx-cli 适配层与真实读取试点闭环
- 明确边界：不做产品判断；不改真实业务数据；不调用外部 LLM；不外发客户原文；不自动回复；不写正式待办池 / 正式日报 / Obsidian 正式区；状态卡和回报队列不写真实消息正文、候选正文、草稿正文、真实会话列表、wxid、key、salt、真实 DB 路径或 daemon 原始日志

## 本轮任务

- 任务标记：`监工派工：2026-05-18｜开发P0返工｜微信反馈防漏收件箱 V1 实战走查 P0 主流程闭环`
- 状态：完成，待测试审查复核
- 结论：已按 P0 主流程闭环返工，不再继续零散堆按钮。前台改为固定左侧导航 + 固定顶部人话状态 + 中间工作区页切换；候选收件箱补中文类型、抽取理由、来源消息计数和固定处理动作；草稿日报在主池为空但最近试读有候选时，默认接上最近试读候选并提示一键生成试读草稿；主流程文案收拢为用户语言，技术字段折叠进诊断详情。
- 回传状态：当前会话无可调用 `send_input` 工具，按协议追加监工回报队列短【完成回报】；未取得 `submission_id`。

## 改动文件

- `/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/routes.py`
- `/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/index.html`
- `/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/app.js`
- `/Users/gd/Desktop/微信agent专项/src/wechat_feedback_app/static/styles.css`
- `/Users/gd/Desktop/微信agent专项/tests/test_real_trial_latest_visibility.py`
- `/Users/gd/Desktop/微信agent专项/tests/test_daily_control.py`
- `/Users/gd/Desktop/微信agent专项/开发/状态卡/开发助手.md`
- `/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`

## P0 主流程闭环

- 固定页切换：左侧导航改为 `今日防漏 / 试读消息 / 候选收件箱 / 群客户打标 / 草稿日报 / 转述摘要 / 配置中心` 的页切换；配置中心和转述摘要从弹窗改为工作区页面；刷新后通过本地状态恢复当前页、筛选、数据源和草稿数据源。
- 顶部人话状态：新增 `服务健康 / 真实读取 / 最近一次可用结果 / 候选事项 / 草稿日报 / 今日收口` 六张状态卡；自动抓取、上次成功、失败原因等进入人话字段；技术状态折叠到 `诊断详情`。
- 候选处理：最近试读候选摘要补 `human_item_type`、`extraction_reason`、`source_message_count`、`detail_actions`；前台详情区固定出现 `确认 / 驳回 / 改类型 / 补充说明 / 撤销`，最近试读候选会提示先加入待确认再处理。
- 草稿日报接线：`draft-preview` 在当前工作台为空且最近试读候选存在时自动选择最近试读候选，返回 `data_source_label`、`suggested_from_trial`、`next_step`；前台可刷新预览 / 重新生成试读草稿，仍只写本地草稿状态。
- 文案替换：主流程可见文案避免把 `workspace / real_trial / binary / formal_write / local_review` 作为用户动作词；旧技术字段仅保留在 API 内部或折叠诊断里。

## 测试 / 证据

- P0 相关专项：`python3 -m pytest tests/test_real_trial_latest_visibility.py tests/test_daily_control.py` 通过，exit=0，日志：`.codex_py_logs/py-run-20260518-161013.log`。
- 全量 pytest：`python3 -m pytest` 通过，62 passed，exit=0，日志：`.codex_py_logs/py-run-20260518-161047.log`。
- 前端脚本语法：`node --check src/wechat_feedback_app/static/app.js` 通过，exit=0。
- `git diff --check`：通过，exit=0。
- 8765 本地 smoke：`http://127.0.0.1:8765/` 硬刷新后 DOM/count-status 级验证通过；命中 V1 标题、7 个固定页导航、6 张顶部状态卡、原始消息 50 / 候选 3 解释、候选动作栏、草稿数据源、转述摘要页、配置中心。未截图，未摘录真实消息正文、候选正文或草稿正文。
- 服务说明：一次使用系统 Python 重启服务的尝试因环境缺少 `uvicorn` 失败，日志：`.codex_py_logs/py-run-20260518-161205.log`；随后 8765 现有本地服务已返回更新后的 `/api/inbox/v1` 和新版前台，smoke 通过。

## 字段白名单

- `/api/inbox/v1` 新增 / 使用字段：`human_status.cards[].key/label/value/hint`、`human_status.auto_fetch`、`human_status.diagnostic_details`、`trial_draft_prompt`、`suggested_draft_data_source`、`workflow_steps`、`group_profile`、`safety`。
- `/api/real-trial/latest/items` 新增 / 使用字段：`human_item_type`、`extraction_reason`、`source_message_count`、`detail_actions`；仍不返回原文证据、raw payload、wxid、key、salt、真实 DB 路径或 daemon 原文。
- 草稿日报预览新增 / 使用字段：`data_source_label`、`suggested_from_trial`、`next_step`、`candidate_count`、`risk_count`、`formal_write_status`、`items[].target/item_id/item_code/item_type/status/risk_level`。
- 前台 smoke 只验证 DOM / count / status / 字段名，不记录真实正文。

## 禁止项确认

- 未执行新的真实读取；未执行 `wx history`、`wx search`、`wx export`、`wx new-messages`。
- 未打开真实 SQLite / exports 正文做人工摘录。
- 未把真实消息正文、候选正文、草稿日报正文、真实会话列表、wxid、key、salt、真实 DB 路径、daemon 原始日志写入状态卡、回报队列、测试日志、README 或外发材料。
- 未调用外部 LLM；未自动回复；未外发；未写正式待办池、正式日报、Obsidian 正式区或外部系统。
- 未持久打开真实读取，默认 `real_read_enabled=false` 保持关闭。

## 剩余风险

- 目前仍是单真实群 / 最近 50 条试读闭环，不包含 P1 的客户系统只读匹配、全局人员库深改、多筛选增强，也不包含 P2 的今日收口增强和完整操作历史。
- 最近试读候选的确认动作仍走“先加入待确认，再在主工作台确认”的保守闭环；没有自动合并，也不写正式区。
- 8765 服务依赖当前本机已有服务环境；系统 Python 直接启动缺少 `uvicorn`，后续若要标准化服务启动，建议另派部署环境修复。
- 当前仓库大量文件为 untracked，按要求未执行 `git add` / `git commit`。

## 下一棒建议

- 派测试审查助手复核：P0 固定页切换、刷新状态恢复、顶部人话状态、候选详情动作、草稿日报接最近试读候选、文案是否仍暴露开发词、字段白名单、默认真实读取关闭、未执行真实读取和正式写入禁用。
- 审查通过后让用户普通刷新 `http://127.0.0.1:8765/`，从 `今日防漏 → 试读消息 → 候选收件箱 → 草稿日报 → 转述摘要` 走一遍主流程。
