# 微信agent专项 开发助手状态卡

更新时间：2026-05-20 15:31 CST

## 线程身份

- 线程名：微信agent专项 开发助手
- 线程 id：019e2a80-0aaa-7ab3-bda0-d7f8e53da989
- 负责范围：本项目实现开发、fixture / real 安全边界、本地 wx-cli 适配层、真实读取试点闭环和发布前置收口核对
- 明确边界：不做产品判断；不改真实业务数据；不调用外部 LLM；不外发客户原文；不自动回复；不写正式待办池 / 正式日报 / Obsidian 正式区；状态卡和回报队列不写真实消息正文、候选正文、草稿正文、真实会话列表、真实成员、客户名单、wxid、key、salt、真实 DB 路径、IP、Windows 敏感路径或 daemon 原始日志

## 本轮任务

- 任务标记：`监工派工：2026-05-20｜开发助手｜轨道B Windows新版同步发布前置收口`
- 状态：缺证据 / 发布前置未通过到可推送状态
- 结论：本轮按要求只读核对，未改业务代码、未部署 Windows、未提交、未推送。当前 `main` 与 `origin/main` 均停在 Windows P0 封版提交 `7babd92`，轨道 B「下一轮日报产品化与配置减负」仍是本机未提交 / 未推送改动，且新增后端测试文件仍为 untracked。轨道 B 代码和测试审查证据可信，但发布前置仍缺“可追踪提交 + 推送前最终检查”。
- 回传状态：当前会话无可调用 `send_input` 工具，已按协议追加监工回报队列；未取得 `submission_id`。

## 只读审查范围

- Git 状态：当前分支 `main`；`HEAD=origin/main=7babd92`；最新提交为 `Seal Windows P0 deployable build`。
- 当前脏改：`AGENTS.md`、`docs/windows-p0-acceptance.md`、`src/wechat_feedback_app/routes.py`、`src/wechat_feedback_app/static/index.html`、`src/wechat_feedback_app/static/app.js`、`src/wechat_feedback_app/static/styles.css`、`开发/状态卡/前台UI助手.md`、`开发/状态卡/后端API助手.md`、`开发/状态卡/测试审查助手.md`、`开发/监工回报队列.md`。
- 当前 untracked：`tests/test_next_round_product_backend.py`。

## 改动归类

- 轨道 B 产品化 / 配置减负代码：`src/wechat_feedback_app/routes.py`、`src/wechat_feedback_app/static/index.html`、`src/wechat_feedback_app/static/app.js`、`src/wechat_feedback_app/static/styles.css`、`tests/test_next_round_product_backend.py`。
- 状态卡 / 回报队列 / 测试审查记录：`开发/状态卡/前台UI助手.md`、`开发/状态卡/后端API助手.md`、`开发/状态卡/测试审查助手.md`、`开发/监工回报队列.md`。
- 协作规则 / 总控直回 / 心跳相关记录：`AGENTS.md` 中新增总控线程 id、总控直回规则和监工 / 总控共享收件口说明；`开发/监工回报队列.md` 中包含总控直回、Windows P0 复验、轨道 B 同步派工和发布前置判断记录。
- Windows 验收文档：`docs/windows-p0-acceptance.md` 补记 Windows P0 整机重启复验通过和轨道隔离说明。

## 已有测试 / 证据核对

- 后端 API 状态卡证据：轨道 B 后端专项、相关 65 项、全量 105 项测试通过；`git diff --check` 通过；8765 reload 和 HTTP smoke 通过；`real_read_enabled=false` 边界已核对。
- 前台 UI 状态卡证据：`node --check src/wechat_feedback_app/static/app.js` 通过；静态资源 diff 检查和全局 `git diff --check` 通过；HTTP count/status smoke 和 DOM 级 smoke 通过。
- 测试审查状态卡证据：测试审查助手复核通过，确认日报产品化、配置减负、群消息先选群、字段白名单、默认真实读取关闭、无真实读取 / 无 roster 同步。
- Windows P0 证据：回报队列已记录 Windows P0 整机重启复验通过，可作为冻结版正式挂机放行；该结论不等于轨道 B 已同步到 Windows。
- 本轮未运行 Python、未新增测试执行；本轮仅做发布前置只读核对和状态记录。

## 发布前建议补跑检查

- `git status --short`：确认待提交范围，尤其确认 `tests/test_next_round_product_backend.py` 被纳入功能提交。
- `git diff --check`：提交前全局空白 / 冲突标记检查。
- `node --check src/wechat_feedback_app/static/app.js`：前台脚本语法检查。
- Python 静默执行：`python3 -m pytest tests/test_next_round_product_backend.py`，stdout/stderr 重定向到 `.codex_py_logs/py-run-YYYYMMDD-HHMMSS.log`，前台仅输出 `python exit=<退出码> log=<日志路径>`。
- Python 静默执行：`python3 -m pytest`，同样写入 `.codex_py_logs/py-run-YYYYMMDD-HHMMSS.log`。
- 8765 运行态 smoke：重载当前代码后只核对 `/api/status`、`/api/daily-center`、`/api/monitor-groups`、`/api/internal-people`、`/api/internal-people/suggestions`、`/api/messages/v1`、`/api/windows-readiness` 的 HTTP status、count/status、error_code 和字段存在性。
- DOM smoke：只验证日报产品化工作台、群配置下拉、我方人员建议、消息先选群、Windows P0 入口不回退；不截图真实内容、不记录正文。
- 配置边界：确认 `config/app.yaml`、`config/app.example.yaml`、`config/app.windows.example.yaml` 中 `real_read_enabled=false` 保持关闭。
- 安全审计：确认未执行新的真实读取、未执行真实 roster 同步、未执行 `wx history/search/export/new-messages`。

## 建议提交清单

- 建议拆成两笔提交，便于 Windows 同步和回滚。
- 第一笔「功能代码 + 测试」：`src/wechat_feedback_app/routes.py`、`src/wechat_feedback_app/static/index.html`、`src/wechat_feedback_app/static/app.js`、`src/wechat_feedback_app/static/styles.css`、`tests/test_next_round_product_backend.py`。
- 第二笔「项目协作文档 / 状态」：`AGENTS.md`、`docs/windows-p0-acceptance.md`、`开发/状态卡/前台UI助手.md`、`开发/状态卡/后端API助手.md`、`开发/状态卡/测试审查助手.md`、`开发/状态卡/开发助手.md`、`开发/监工回报队列.md`。
- 如果总控要求单笔，也可合并为一笔发布候选提交，但不如两笔清晰。

## 建议提交信息

- 第一笔：`Add next-round daily center and config relief workbench`
- 第二笔：`Record Windows P0 acceptance and release handoff state`
- 单笔备选：`Prepare track B Windows release handoff`

## 推送前必须满足

- 轨道 B 功能代码、测试文件、必要状态 / 文档文件已被明确选择并提交；不得漏掉 untracked 测试文件。
- `git diff --check` 通过。
- 后端专项测试、全量 pytest、前台 `node --check` 通过，Python 日志遵守静默规则。
- 8765 运行态 HTTP / DOM smoke 通过，且只记录 count/status/error_code/字段存在性。
- `real_read_enabled=false` 保持关闭；无新增真实读取、无真实 roster 同步、无 forbidden wx 命令。
- 状态卡、回报队列、测试日志和提交说明不包含真实消息、候选、草稿、会话、成员、客户名单、wxid/key/salt、真实 DB 路径、IP、Windows 敏感路径或 daemon 原始日志。

## 发布前置结论

- 结论：缺证据 / 不通过到可发布状态。
- 原因：轨道 B 测试审查已通过，但当前工作区仍是本机脏改，`main` 与 `origin/main` 仍停在 Windows P0 封版提交；未形成 Windows 可拉取的提交版本；本轮也未补跑发布前最终检查。
- 下一棒建议：先由总控明确授权「补跑发布前检查 + 提交 / 推送」。授权后建议按两笔提交执行，再派 Windows 新版同步 / 复验。若总控要求先补测，则先跑上方发布前检查清单。

## 禁止项确认

- 本轮未执行新的真实读取；未执行真实 roster 同步。
- 未执行 `wx history`、`wx search`、`wx export`、`wx new-messages`。
- 未摘录真实消息、候选、草稿、真实会话、真实成员、客户名单、wxid/key/salt、真实 DB 路径、IP、daemon 原始日志或 Windows 具体敏感路径。
- 未写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 未提交、未推送、未部署 Windows、未改业务代码。

## 剩余风险

- 轨道 B 未提交 / 未推送前，Windows 端没有可信可拉取来源，不能进入新版部署复验。
- 当前证据来自后端、前台和测试审查状态卡 / 回报队列；发布前仍建议补跑最终检查，避免脏改过程中出现漂移。
- `tests/test_next_round_product_backend.py` 当前为 untracked，若提交时遗漏，会造成发布候选缺少关键测试证据。

## 下一棒建议

- 如果目标是尽快同步 Windows：派提交 / 推送棒，先补跑发布前检查，再按两笔提交落地并推送。
- 如果目标是更稳：先派补测棒，只跑发布前检查清单并更新证据，再决定提交 / 推送。
- 推送后再派 Windows 新版同步 / 复验，只验证 HTTP status、count/status、error_code 和字段存在性，不扩大真实读取或 roster 同步范围。
