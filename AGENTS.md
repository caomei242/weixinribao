# 微信agent专项协作规则

本项目路径：`/Users/gd/Desktop/微信agent专项`

当前监工线程 id：`019e2a4a-664f-76b3-9533-fbbbfc7f34c5`
当前监工线程深链：`codex://threads/019e2a4a-664f-76b3-9533-fbbbfc7f34c5`

## 线程分工

- 监工线程只做规划、派工、进度监控、验收、返工、文档收口和状态沉淀，默认不亲自改业务代码。
- 固定工人线程必须登记在 `开发/固定线程登记表.md`，不得跨项目复用同名助手。
- 工人线程完成任务时，先写自己的状态卡，再优先用 `send_input(target="019e2a4a-664f-76b3-9533-fbbbfc7f34c5")` 向监工线程短回 `【完成回报】`。
- 回传成功后，工人线程必须在状态卡、回报队列或最终回复里写明 `已发监工：<submission_id>`；`submission_id` 只算回报投递记录，不算验收通过。
- 如果 `send_input` 不可用或失败，再追加一条到 `开发/监工回报队列.md`，并说明未发出原因。
- 直接聊天回监工线程是主动唤醒通道；状态卡和回报队列是监工验收兜底入口。
- 监工验收只读状态卡、测试结果、截图路径、改动文件列表和剩余风险。

## 数据边界

- 开发需求收件箱：`/Users/gd/Desktop/微信agent专项/开发/开发需求收件箱.md`
- 固定线程登记表：`/Users/gd/Desktop/微信agent专项/开发/固定线程登记表.md`
- 状态卡目录：`/Users/gd/Desktop/微信agent专项/开发/状态卡/`
- 监工回报队列：`/Users/gd/Desktop/微信agent专项/开发/监工回报队列.md`

## 客户识别与客户源边界

- 2026-05-20 起，群管理客户识别可以消费本地配置客户与草莓客户管理系统只读客户源，但前台 / 后端 / 测试回报只允许记录 `source_status`、`source_label`、`customer_options_count`、`match_status`、`reason_code`、HTTP status 和布尔结果。
- 草莓客户管理系统客户源必须走只读接入，优先复用 `/Users/gd/Desktop/主业--草莓客户管理系统` 现有读取模块 / Markdown store；不要在本项目新建平行客户库。
- 如果客户系统源缺失、不可读或未接通，API 必须返回 `source_status` / `source_error_code` 级摘要，让前台提示客户系统源异常；不得把本项目本地少量客户选项伪装成完整客户库。
- 状态卡、回报队列、测试日志和 README 不得摘录真实群名、真实客户名单、真实消息正文或真实会话列表。
- 客户识别交互必须保留三态：可靠命中自动选中；有 suggestion 但不可靠时显示“疑似客户需确认”并允许采用建议；无 suggestion 或客户源不可用时显示客户源未接通 / 不可读 / 客户选项不足等人话提示。
- 保存监控群时仍使用 `customer_name` / `customer_id` 字段，完成后从后端读回展示；不得为了证明链路可用而在文档或回报里写真实客户名。

## 微信群 roster 同步边界

- 2026-05-20 起，微信群完整成员名单只能通过显式授权的 roster 同步获取；未授权接口只能返回 `authorization_required`、`full_sync_requires_authorization`、`no_roster_read_executed` 等摘要字段，不得返回成员名单明细。
- 测试审查不得执行真实授权 roster 同步；如需验收，只能用 fake / stub、HTTP status、count、字段存在性和敏感命中结果。
- 新增监控群的“保存后同步微信群全员名单”必须有二次确认或等价授权说明；读取范围限成员 roster 元数据，不读取聊天消息，不外发，不写正式日报、正式待办池、Obsidian 正式区或外部系统。
- 已有群只允许用户显式点击手动同步；打开编辑页、刷新页面或保存普通配置不得自动读取 roster。

## Windows P0 验收与运行态规则

- 2026-05-20 起，Windows 可实战第一版验收不只看单元测试或源码静态证据，必须按用户路径复核：打开系统、看 Windows readiness、配置监控群、配置我方人员、按群查看消息、看到候选、生成日报。
- 后端/API 变更后必须重启或重载 8765 运行态，并补运行态 HTTP smoke；源码测试通过但 127.0.0.1:8765 仍返回旧接口 / 404 时，不得判通过。
- 前台/UI 必须消费后端主路径接口，不得只靠浏览器环境、旧本地状态或 fixture 兜底判断。Windows P0 主路径至少包括 `/api/windows-readiness`、`/api/monitor-groups`、`/api/internal-people`、`/api/messages/v1`、`/api/daily-center/generation-status`。
- 消息明细必须先选群再看；单群 0 条时必须显示空态或切换提示，禁止回退展示全部群消息造成多群混排。
- 日报中心首屏要做视觉验收：标题 / 说明、操作按钮、状态 / KPI 卡片、日报全文区域不得挤压、覆盖或压线；按钮组必须能换行；至少检查 2048x1152、1440x900、窄屏三档。
- 测试审查和监工回报只写 count / status / 字段存在性 / 布尔结果 / 截图路径，不摘录真实消息、成员名单、客户名单或路径敏感信息。
- 用户截图指出明显视觉或主流程问题时，监工必须暂停“最终通过”口径，先派最小返工，再合并复验。
- 2026-05-20 Windows P0 代码侧通过测试审查后，下一步顺序固定为：产品助手轻量走查用户路径，再派 Windows 实机部署 / 复验；不得把 Mac 开发机通过当作 Windows 实机通过，也不得在实机复验前混入 Slock Agent 多人操作或新一轮大需求。

## 固定线程回报协议

工人线程完成、卡住、返工或发现越界风险时，必须在 `开发/监工回报队列.md` 追加一条回报块。回报块格式固定：

```markdown
## 2026-05-15 HH:MM｜线程名｜任务标记

- 状态：完成 / 收下 / 返工 / 卡住 / 缺证据
- 线程 id：
- 状态卡：
- 测试 / 证据：
- 改动文件 / 审查范围：
- 剩余风险：
- 下一棒建议：
```

监工心跳优先读取回报队列中最新未处理条目，再读对应状态卡和证据文件。监工处理后，在该条目下追加：

```markdown
- 监工处理：已验收 / 已返工 / 已派下一棒
- 处理时间：2026-05-15 HH:MM CST
```

如果工人线程能直接短回监工线程，仍然可以发送 `【完成回报】`，但这只是加速提醒；不得替代状态卡和回报队列。

如果工人线程可用 `send_input`，必须优先使用：

```text
send_input(target="019e2a4a-664f-76b3-9533-fbbbfc7f34c5", message="【完成回报】...")
```

并记录：

```text
已发监工：<submission_id>
```

## Python 静默日志

运行任何 `python`、`python3`、`python -m`、`python3 -m` 或等价 Python 命令时，必须创建 `.codex_py_logs/`，并把 stdout/stderr shell 级重定向到 `.codex_py_logs/py-run-YYYYMMDD-HHMMSS.log`。

前台只允许输出：

```text
python exit=<退出码> log=<日志路径>
```

只有排障需要时，才读取日志关键片段。
